#include "main.h"

/**
 * main - tests function that prints if integer is positive or negative
 *
 * Return: Always 0
 */
int main(void)
{
    int i;

    printf("Infinite loop incoming :(\n");

    i = 0;

    /* Commented out the code that caused the infinite loop
     * while (i < 10)
     * {
     *     putchar(i);
     * }
     */

    printf("Infinite loop avoided! \\o/\n");

    return (0);
}
