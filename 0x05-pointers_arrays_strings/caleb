#include "main.h"
#include <stdio.h> /* Include the stdio.h header to resolve printf function */

/**
 * _puts - Prints a string, followed by a new line, to stdout.
 * @str: The string to be printed.
 */
void _puts(char *str)
{
    printf("%s\n", str);
}
